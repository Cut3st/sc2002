import ui.CLI;
import combat.BattleEngine;

public class Main {
    public static void main(String[] args) {
        CLI cli = new CLI();
        // GameController gc = new GameController(cli, new BattleEngine());
        // gc.run();
    }
}
